# Automation: Physical button triggers Wake Up scene
# Paste into automations.yaml on HA instance
# Reload: http://homeassistant.local:8123/developer-tools/yaml (Automations)
# Discover device_id by pressing the button and checking http://homeassistant.local:8123/logbook
# and doing an automation http://homeassistant.local:8123/config/automation/edit/new

- id: wake_up_button
  alias: Wake Up Button
  description: ""
  mode: single
  triggers:
    - device_id: 0033cb20eaa852bf8fbfa9b6bbd766cc
      domain: hue
      type: long_press
      subtype: 3
      # unique_id: REPLACE_WITH_BUTTON_3_UNIQUE_ID, optional but can help avoid issues if you change the button later
      trigger: device
  conditions: []
  action:
    - action: scene.turn_on
      target:
        entity_id: scene.wake_up_ha

# --- Alternative B: Zigbee button via Zigbee2MQTT (e.g. IKEA, Aqara) ---
#   trigger:
#     - platform: mqtt
#       topic: zigbee2mqtt/YOUR_BUTTON_NAME/action
#       payload: single   # or "double", "long" depending on your button

# --- Alternative C: Any generic Zigbee button via device trigger ---
#   trigger:
#     - platform: device
#       device_id: YOUR_DEVICE_ID
#       domain: zha          # or mqtt
#       type: remote_button_short_press
#       subtype: button_1
